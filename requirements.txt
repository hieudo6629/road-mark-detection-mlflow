# ======================
# PyTorch CPU only (ổn định, nhẹ)
# ======================
torch
torchvision
--extra-index-url https://download.pytorch.org/whl/cpu

# ======================
# YOLO (phiên bản ổn định, không quá mới để tránh bug)
# ======================
ultralytics==8.0.196

# ======================
# MLOps (MLflow 2.x ổn định, tương thích code hiện tại)
# ======================
mlflow==2.5.0
boto3==1.28.57
prometheus-client==0.17.1

# ======================
# API (FastAPI + Uvicorn ổn định)
# ======================
fastapi==0.103.2
uvicorn==0.23.2
python-multipart==0.0.6
pillow==10.0.0